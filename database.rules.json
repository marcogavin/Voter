{
  "rules": {
    "events": {
      "$eventId": {
        ".read": "auth != null",

        "ownerUid": {
          ".write": "auth != null && newData.val() === auth.uid && (!data.exists() || data.val() === auth.uid)",
          ".validate": "newData.isString()"
        },

        "currentIndex": {
          ".write": "auth != null && (!data.parent().child('ownerUid').exists() || data.parent().child('ownerUid').val() === auth.uid)",
          ".validate": "newData.isNumber()"
        },

        "revealed": {
          ".write": "auth != null && (!data.parent().child('ownerUid').exists() || data.parent().child('ownerUid').val() === auth.uid)",
          ".validate": "newData.isBoolean()"
        },

        "questions": {
          ".write": "auth != null && (!data.parent().child('ownerUid').exists() || data.parent().child('ownerUid').val() === auth.uid)",

          "$questionId": {
            "text": {
              ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 200"
            },

            "correct": {
              ".validate": "newData.isString() && newData.val().length <= 4"
            },

            "options": {
              "$optionId": {
                "label": {
                  ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 100"
                },
                "$otherField": {
                  ".validate": false
                }
              }
            },

            "voters": {
              "$voterUid": {
                ".write": "auth != null && $voterUid === auth.uid && root.child('events').child($eventId).child('revealed').val() !== true",
                ".validate": "newData.isString() && newData.val().length <= 4"
              }
            },

            "$otherField": {
              ".validate": false
            }
          }
        },

        "$otherField": {
          ".validate": false
        }
      }
    }
  }
}
